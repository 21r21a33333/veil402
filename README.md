# Veil402

Private agent payments on Solana: **private by default, provable on demand**.
A shielded UTXO pool (amount + recipient anonymity) with per-transaction, per-verifier
selective disclosure for single-tx auditability.

> Status: **research + spec phase**. No implementation yet — see `docs/`.
